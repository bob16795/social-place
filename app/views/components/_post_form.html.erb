<div class="post-form">
  <%= form_for(@newPost) do |f| %>

    <div class="field">
      <%= f.text_area :content, :rows => "3", autofocus: true %>
    </div>

    <div class="actions">
      <%= f.submit "Post" %>
    </div>
  <% end %>
</div>
