<%= form_for(@newPost) do |f| %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, :rows => "3", autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Post" %>
  </div>
<% end %>
